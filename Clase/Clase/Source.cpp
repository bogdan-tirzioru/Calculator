#include "Header.h"



tip calcule::suma(tip a, tip b)
{
	return a + b;
}

tip calcule::diferenta(tip a, tip b)
{
	return a - b;
}

tip calcule::inmultire(tip a, tip b)
{
	return a * b;
}

tip calcule::impartire(tip a, tip b)
{
	return a / b;
}

